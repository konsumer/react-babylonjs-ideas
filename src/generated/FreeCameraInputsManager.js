import React from 'react'

class FreeCameraInputsManager extends React.Component {
  constructor (props) {
    super(props)
    const {  } = props
    this.FreeCameraInputsManager = new FreeCameraInputsManager()
  }

  render () {
    return null
  }
}

export default FreeCameraInputsManager

