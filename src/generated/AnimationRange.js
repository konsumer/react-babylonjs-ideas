import React from 'react'

class AnimationRange extends React.Component {
  constructor (props) {
    super(props)
    const { name, from, to } = props
    this.AnimationRange = new AnimationRange(name, from, to)
  }

  render () {
    return null
  }
}

export default AnimationRange

