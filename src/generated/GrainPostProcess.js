import React from 'react'

class GrainPostProcess extends React.Component {
  constructor (props) {
    super(props)
    const { intensity, animated } = props
    this.GrainPostProcess = new GrainPostProcess(intensity, animated)
  }

  render () {
    return null
  }
}

export default GrainPostProcess

